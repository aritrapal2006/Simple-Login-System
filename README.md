# Simple-Login-System
Create a basic login system that:  Allows registration. Takes username and password. Saves credentials to a file. Allows login using saved credentials. Displays success/failure.
